/**
 * すべてを実行するクラス
 */
class Main {

  /**
   * @param {JSON} event 
   */
  constructer(event) {
    this.event = event;
  }

  /**
   * 実行
   */
  execute() {
    let result;

    switch (this.event.type) {
      case "follow":

      case "unfollow":

      case "message":

    }

    return result;
  }
}